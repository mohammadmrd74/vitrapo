:80 {
    # Backend API
    handle /api/* {
        reverse_proxy nestapp:4000
    }

    # Uploaded files (served by Nest static handler)
    handle /files/* {
        reverse_proxy nestapp:4000
    }

    # Frontend SPA (fallback)
    handle {
        reverse_proxy frontapp:3000
    }
}
