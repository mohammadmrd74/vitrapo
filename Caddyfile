http://crm.vitrapo.com {
    handle /api/* {
        reverse_proxy localhost:4000
    }

    handle /files/* {
        reverse_proxy localhost:9000
    }

    handle {
        reverse_proxy localhost:3000
    }
}